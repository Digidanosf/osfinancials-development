CREATE TABLE EVENTSPECIALDAYS
(
  WEVENTSPECIALDAY_ID Integer NOT NULL,
  WTYPEID Integer,
  WRESOURCEID Integer,
  WCOLOR Integer,
  WPRIORITY Integer,
  DSTARTDATETIME {timestamptype},
  DENDDATETIME {timestamptype},
  SDESCRIPTION varchAr(50),
  PRIMARY KEY (WEVENTSPECIALDAY_ID)
)
;
ALTER TABLE EVENTSPECIALDAYS ADD CONSTRAINT FK_EVENTSPECIALDAYS_EVENT
  FOREIGN KEY (WRESOURCEID) REFERENCES RESOURCES (RESOURCEID) ON UPDATE CASCADE ON DELETE CASCADE
;{if InterBase}
UPDATE RDB$RELATION_FIELDS set RDB$DESCRIPTION = '1 = Weekday user 2 = From/ to datetime for All users 
3 = From/To datetime this calender 
'  where RDB$FIELD_NAME = 'WTYPEID' and RDB$RELATION_NAME = 'EVENTSPECIALDAYS'
{endif};
{generator_cr_start}GEN_EVENTSPECIALDAYS_ID{generator_cr_end}
;
update Sysvars set WVersion=534
;





