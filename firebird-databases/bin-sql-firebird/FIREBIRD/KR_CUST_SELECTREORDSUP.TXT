select coalesce(se.wunitid,Stock.WUnitID) WUnitID, stock.* from stock left join STOCK_EXTRADATA se on se.wstockid = stock.wstockid 
where FReorderQty <> -1 and Bdisabled = 0 and WStockTypeID not in (1,2,4,6,8) and   stock.WSUPPLIER1ID = <@ACC@>
 
